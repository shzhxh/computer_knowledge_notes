#### 概述

声明了一些数学函数和宏。

```c
/* double型浮点数的运算：x*y+z */
double fma(double x, double y, double z);
/* float型浮点数的运算：x*y+z */
float fmaf(float x, float y, float z);
/* long double型浮点数的运算：x*y+z */
long double fmal(long double x, long double y, long double z);
```

