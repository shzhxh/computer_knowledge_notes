```
git clone [options] <repository> [directory]
```

#### 选项

```bash
-b, --branch <name>	# 指向name所代表的分支或标签。
--origin <name>, -o <name>	# 用<name>追踪远程仓库，默认是origin
--depth <N>	# 使用浅层克隆，其深度为<N>
--recurse-submodules[=pathspec]	# clone完成后，继续初始化和克隆子模块。
```

