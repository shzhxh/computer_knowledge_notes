```
do-release-upgrade [option]	# 把操作系统升级到最新版本
```

#### 选项

```
-d, --devel-release	# 如果当前版本是最新版本，则会升级到开发版
-p, --proposed	# 使用Ubuntu建议的upgrader升级
-m <MODE>, --mode=<MODE> # 指定升级的模式。当前支持两种模式：desktop和server。
-f <FRONTEND>, --frontend=<FRONTEND>	# 运行指定的前端
```

