```
losetup [options] [loopdev]		# 设置loop设备

```

#### 选项

```
-d, --detach <loopdevs>	# 解除到loopdev的连接.
-f, --find [file]	# 查找空闲的loop设备.如指定[file],则将[file]关联到那个空闲的loop设备.
-l, --list 	# 
-P, --partscan	# 在新创建的loop设备上，强制内核扫描分区表
```

