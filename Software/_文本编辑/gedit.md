#### 用法

```
gedit [option] [FILE] [+LINE[:COLUMN]]	# 打开文件
gedit [option] -	# 从标准输入读取
```

#### 选项

```
--encoding	# 指定字符编码
--list-encodings	# 列出可用的字符编码
--new-window
--new-document
-s, --standalone
-w, --wait
--help		# 打印帮助信息
--version	# 打印版本信息
```

