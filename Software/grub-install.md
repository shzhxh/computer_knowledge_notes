```
grub-install [options] [DEVICE]	# 安装grub
```

#### 选项

```
-d, --directory=<DIR>	# 使用DIR目录下的镜像和模块，默认为/usr/lib/grub/<platform>
--efi-directory=<DIR>	# 把DIR作为EFI系统分区
--target=<TARGET>		# 基于TARGET平台安装grub，默认为x86_64-efi 
```

