#### 选项

```
-m, --mount	# 挂载指定的目标。
-u, --unmount	# 解除挂载指定的目标。
-l, --list
-c, --change-password	# 修改密码。
-g, --generate-key <size>	# 设置密钥。
-p, --prepare	# 准备好设备但不挂载，用于安装文件系统。
-r, --release	# 释放设备。
```

